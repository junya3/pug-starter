extends /component/pug/layout/_layout

include /component/pug/ui/_page-title

block prepend title
  - const pageTitle = "top";

block content
  +page-title(title)
  section
    h2.sec-title コンポーネント読み込み
    p pug：#[code include /component/pug/_filename]
    p 下層ページもルートパスで指定してください。
      a(href="/about/") サンプル
    p scss：#[code @use 'style/_global/g-mixin' as *;]など

    p image：#[code img(src='/assets/image/sample.png' alt='')]

  section
    h2.sec-title 使い方

    p #[code src/app]にページの管理、#[code src/component]にコンポーネントの管理をしてください。
    p 開発時には#[code npm run watch]を使用してください。
    p #[code pug],#[code scss],#[code typescript]を監視しながらlive-serverを立ち上げることで擬似的にホットリロードを実現しています。
    p 監視する対象を#[code npm run watch:pug]など、対象を制限することもできます。
    p 通常の監視でも#[code dist]にコピーされますが、画像のみをdistに格納する場合は#[code npm run copy:images]としてください。
    p 単独でlive-serverを立てる場合は#[code npm run serve]を使用してください。
    p コンパイルされたものはdistの中に格納されます。サーバーに上げる際はそちらをご利用ください。
